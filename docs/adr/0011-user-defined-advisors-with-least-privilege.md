# ADR-0011：用户可定义参谋，但遵循最小权限

- 状态：Accepted
- 日期：2026-08-18

## 背景

企业需要领域参谋访问私有技能和内部 API，但自由组合会造成越权、秘密泄漏和职责混乱。

## 决策

Advisor Profile 显式声明职责/非职责、领域标签、模型、推理、工具、技能、API grants、数据分类、凭据引用、预算和生命周期。路由先做确定性资格过滤，再做语义排序。

## 后果

描述文本不授予权限；权限变化需要 revision、审计和 canary。
