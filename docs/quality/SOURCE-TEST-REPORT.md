# Source Test Report

Baseline: dsh `0.1.1-rc.2` @
`b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`

Result: **PASS**

- Tests: `63`
- Passed: `63`
- Failed: `0`
- Skipped: `0`
- Cancelled: `0`

Coverage includes contracts, Mission UoW, durable state, fault injection,
SQLite restart, authorization/budget settlement, Git/worktree integration,
reconciliation, automation outbox, WebUI behavior, Preset generation,
Installer and real installed RC.2 Profile execution.

The Profile E2E boots the official RC.2 Loader three times and covers Settings,
Web Client registration, Tool execution, a continuable Worker, Mission → Task
→ Candidate → Verification → Integration, fabricated evidence rejection,
durable duplicate commands and final recovery. It uses a deterministic
in-process LLM adapter; external Provider credentials and network behavior are
deployment checks.

The machine-readable source is the root `TEST-REPORT.json`, regenerated by
`pnpm test`.
