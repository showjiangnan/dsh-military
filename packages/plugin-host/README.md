# @dsh-military/plugin-host

Host service graph and thin DeepSeek Harness `0.1.1-rc.2` adapters. Host listeners fail closed and immediately delegate for sessions whose actual preset is not `military`.
