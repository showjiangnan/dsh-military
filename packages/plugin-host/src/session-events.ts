/**
 * RC.2 deliberately has no public runtime registration seam for required
 * out-of-repository Session event types. dsh-military therefore keeps Mission
 * facts in its own Ledger and uses only upstream-known DSH events/messages in
 * Session logs. This module remains as a compatibility import with no merges.
 */
export {}
