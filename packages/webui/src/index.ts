import type { Context } from '@deepseek-ai/cordis'

/** Host half is intentionally thin: plugin-host owns durable settings and APIs. */
export const name = 'dsh-military-webui-host'
export function apply(_ctx: Context): void {}
